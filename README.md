Автоматизований скрапер для збору інформації про вживані автомобілі з сайту [AutoRia.com](https://auto.ria.com/). Проєкт створено як частина тестового завдання для позиції Junior Python Developer.

## 📋 Зміст

- [Опис](#-опис)
- [Основні можливості](#-основні-можливості)
- [Технології](#-технології)
- [Структура проєкту](#-структура-проєкту)
- [Встановлення та запуск](#-встановлення-та-запуск)
- [Конфігурація](#-конфігурація)
- [Структура бази даних](#-структура-бази-даних)
- [Автоматизація](#-автоматизація)
- [Особливості реалізації](#-особливості-реалізації)

## 📖 Опис

AutoRia Scraper - це інструмент для автоматичного збору та зберігання інформації про вживані автомобілі з сайту AutoRia. Програма щодня сканує оголошення, витягує детальну інформацію про кожен автомобіль та зберігає її в базі даних PostgreSQL.

**🔗 [Посилання на технічне завдання](https://docs.google.com/document/u/0/d/1g4g2s2118ehKZ_GjpqHjcDfSfSzH4Lx2gHv84rPOOr8/mobilebasic)**

## ✨ Основні можливості

- ✅ Автоматичний щоденний скрапінг оголошень о 12:00
- ✅ Збір детальної інформації про автомобілі (включаючи VIN-код)
- ✅ Отримання контактних даних продавців через API
- ✅ Збереження даних в PostgreSQL з уникненням дублікатів
- ✅ Автоматичне створення резервних копій БД щодня о 00:00
- ✅ Контейнеризація через Docker
- ✅ Логування всіх операцій

## 🛠 Технології

- **Python 3.11+** - основна мова програмування
- **Scrapy 2.11+** - фреймворк для веб-скрапінгу
- **PostgreSQL 16** - система управління базами даних
- **SQLAlchemy** - ORM для роботи з базою даних
- **Poetry** - менеджер залежностей
- **Docker & Docker Compose** - контейнеризація
- **APScheduler** - планувальник задач
- **python-dotenv** - управління змінними оточення

## 📁 Структура проєкту

```
autoria-scraper/
├── autoria/                    # Scrapy проєкт
│   ├── spiders/               # Спайдери для скрапінгу
│   ├── items.py               # Визначення структури даних
│   ├── middlewares.py         # Проміжні обробники
│   ├── pipelines.py           # Обробка та збереження даних
│   └── settings.py            # Налаштування Scrapy
├── database/                  # Модулі для роботи з БД
│   ├── models.py             # ORM моделі
│   └── connection.py         # Підключення до БД
├── scripts/                   # Допоміжні скрипти
│   └── dump_db.sh            # Скрипт для створення дампу БД
├── dumps/                     # Резервні копії бази даних
├── config.py                  # Конфігурація додатку
├── main.py                    # Точка входу програми
├── docker-compose.yml         # Конфігурація Docker Compose
├── Dockerfile                 # Інструкції для Docker образу
├── pyproject.toml            # Конфігурація Poetry
├── requirements.txt          # Залежності проєкту
└── .env.example              # Приклад змінних оточення
```

## 🚀 Встановлення та запуск

### Передумови

- Docker та Docker Compose встановлені на вашій системі
- Git для клонування репозиторію

### Кроки встановлення

1. **Клонуйте репозиторій:**
```bash
git clone https://github.com/TT1410/autoria-sraper.git
cd autoria-scraper
```

2. **Створіть файл `.env` на основі `.env.example`:**
```bash
cp .env.example .env
```

3. **Відредагуйте `.env` файл** та вкажіть ваші налаштування:
```env
# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=autoria_db
DB_USER=postgres
DB_PASSWORD=your_secure_password

# Application
TZ=Europe/Kyiv
```

4. **Запустіть проєкт через Docker Compose:**
```bash
docker-compose up -d
```

5. **Перевірте статус контейнерів:**
```bash
docker-compose ps
```

### Зупинка проєкту

```bash
docker-compose down
```

### Перегляд логів

```bash
# Всі логи
docker-compose logs

# Логи конкретного сервісу
docker-compose logs scraper
docker-compose logs postgres
```

## ⚙️ Конфігурація

### Змінні оточення (.env)

| Змінна | Опис | Значення за замовчуванням |
|--------|------|---------------------------|
| `DB_HOST` | Хост бази даних | `postgres` |
| `DB_PORT` | Порт бази даних | `5432` |
| `DB_NAME` | Назва бази даних | `autoria_db` |
| `DB_USER` | Користувач БД | `postgres` |
| `DB_PASSWORD` | Пароль БД | - |
| `TZ` | Часова зона | `Europe/Kyiv` |

### Налаштування скрапінгу

Основні параметри скрапінгу можна змінити у файлі `autoria/spiders/auto_spider.py`:

```python
filters = {
    "indexName": "auto",                      # Б/У автомобілі
    "sort[0].order": "dates.created.desc",    # Сортування за датою
    "dates.sold.not": "0000-00-00 00:00:00",  # Без проданих авто
    "size": 100,                              # 100 елементів на сторінці
    "page": 0,                                # Номер сторінки
}
```

## 💾 Структура бази даних

### Таблиця `cars`

| Поле | Тип | Опис | Обмеження |
|------|-----|------|-----------|
| `id` | `SERIAL` | Первинний ключ | `PRIMARY KEY` |
| `url` | `VARCHAR(500)` | Посилання на оголошення | `UNIQUE, NOT NULL` |
| `title` | `VARCHAR(255)` | Назва авто | `NOT NULL` |
| `price_usd` | `INTEGER` | Ціна в доларах | `NOT NULL` |
| `odometer` | `INTEGER` | Пробіг в км | `NULLABLE` |
| `username` | `VARCHAR(255)` | Ім'я продавця | `NULLABLE` |
| `phone_number` | `BIGINT` | Номер телефону | `NOT NULL` |
| `image_url` | `TEXT` | Посилання на фото | `NULLABLE` |
| `images_count` | `INTEGER` | Кількість фото | `DEFAULT 0` |
| `car_number` | `VARCHAR(20)` | Номерний знак | `NULLABLE` |
| `car_vin` | `VARCHAR(17)` | VIN-код | `NOT NULL` |
| `datetime_found` | `TIMESTAMP` | Дата додавання | `DEFAULT NOW()` |

### Особливості полів

- **`url`**: Унікальне поле, що запобігає дублюванню оголошень
- **`odometer`**: Може бути `NULL`, якщо пробіг не вказаний
- **`username`**: Може бути `NULL`, якщо ім'я продавця не вказане
- **`phone_number`**: Зберігається без префіксу `+38`
- **`image_url`**: Посилання на перше фото з оголошення
- **`car_number`**: Може бути `NULL`, якщо номер не вказаний
- **`car_vin`**: Обов'язкове поле; оголошення без VIN пропускаються

## ⏰ Автоматизація

Проєкт використовує APScheduler для автоматизації задач:

### Розклад виконання

| Час | Задача | Опис |
|-----|--------|------|
| 12:00 | Скрапінг | Збір нових оголошень з AutoRia |
| 00:00 | Резервна копія | Створення дампу бази даних |

### Створення дампів БД

Резервні копії зберігаються у папці `/dumps` з іменами у форматі:
```
dump_YYYY-MM-DD_HH-MM-SS.sql
```

Приклад: `dump_2024-01-15_00-00-00.sql`

## 🔍 Особливості реалізації

### Вибір стартового URL

Замість URL з технічного завдання (`https://auto.ria.com/car/used/`), використовується:
```
https://auto.ria.com/uk/search/
```

**Переваги:**
- Можливість використання query-параметрів для фільтрації
- Можливість встановлення кількості елементів на сторінці (до 100)
- Значне прискорення роботи скрапера
- Зменшення кількості HTTP-запитів

### Отримання номерів телефонів

Номери телефонів отримуються через API запит до AutoRia:
- Параметри запиту витягуються з HTML оголошення
- Запит імітує натискання кнопки "Показати номер"
- Зберігається лише основний номер телефону

**Альтернатива**: Можна було використати Selenium, але це значно сповільнило б роботу програми.

### Фільтрація оголошень

Оголошення без VIN-коду автоматично пропускаються, оскільки:
- Такі оголошення часто стосуються причепів, катерів тощо
- VIN-код є важливим ідентифікатором автомобіля
- Це допомагає підтримувати якість даних у БД

### Унікальність записів

- Кожне оголошення унікальне завдяки полю `url`
- При повторному скрапінгу дублікати ігноруються
- Видалені оголошення також пропускаються
- Старі записи залишаються незмінними

## 📊 Моніторинг та налагодження

### Перегляд логів

```bash
# Логи скрапера
docker-compose logs -f scraper

# Логи бази даних
docker-compose logs -f postgres
```

### Підключення до БД

```bash
docker-compose exec postgres psql -U postgres -d autoria_db
```
